<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for downloading models from HuggingFace -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Required for background_downloader foreground service (large model downloads).
         FOREGROUND_SERVICE_DATA_SYNC maps to the data-sync foreground service type,
         which is appropriate for long-running network file transfers. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- Optional OpenCL libraries for GPU acceleration in LiteRT inference.
             Must be inside <application>; marked required=false so the app
             installs on devices without OpenCL. -->
        <uses-native-library android:name="libOpenCL.so"       android:required="false"/>
        <uses-native-library android:name="libOpenCL-car.so"   android:required="false"/>
        <uses-native-library android:name="libOpenCL-pixel.so" android:required="false"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Required on Android 14+ (API 34): foreground service type must match
             the declared FOREGROUND_SERVICE_DATA_SYNC permission.
             background_downloader auto-starts this service for large downloads;
             declaring it here ensures the foregroundServiceType attribute is set
             even if the plugin's own manifest merge is incomplete. -->
        <service
            android:name="com.bbflight.background_downloader.TaskWorker"
            android:foregroundServiceType="dataSync"
            android:exported="false"/>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
