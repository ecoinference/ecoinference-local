package com.gemma4.aiserver

import io.flutter.embedding.android.FlutterActivity

// flutter_gemma registers its own plugin automatically — no manual wiring needed.
class MainActivity : FlutterActivity()
